services:
  db:
    image: postgres:18
    container_name: xg_postgres
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - xg_pg_data:/var/lib/postgresql
    restart: unless-stopped

volumes:
  xg_pg_data: